import React, {Component} from "react"

export default class NoFound extends Component{
    render(){
        return (
            <div>
                Can't view the secret page unless logged-in
            </div>
        )
    }
}